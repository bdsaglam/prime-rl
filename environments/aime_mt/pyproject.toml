[project]
name = "aime-mt"
description = "AIME math competition environment with reflection-in-sequence (multi-turn)"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "verifiers>=0.1.9",
    "datasets",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
