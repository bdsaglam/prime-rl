[project]
name = "aime"
description = "AIME math competition environment"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "verifiers>=0.1.9",
    "datasets",
    "litellm",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
