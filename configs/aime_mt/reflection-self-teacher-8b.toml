# Reflection-in-Sequence: Self-Teacher OPD
# 8B student + 8B self-teacher with multi-turn reflection
# Student solves → told correct/incorrect → reflects in structured format
# Teacher PI = student reflection + answer + correct sibling (assembled by env)
# Train on AIME 2025 problems 0-21, eval on 22-29 (non-overlapping)
# Pure OPD (adv_tau=0, teacher_tau=1)
# 4x A100 80GB: 1 train + 2 infer + 1 teacher

max_steps = 20
seq_len = 32768
output_dir = "outputs/aime-mt-reflection-pi-8b-v4"

[deployment]
num_train_gpus = 1
num_infer_gpus = 2
num_teacher_gpus = 1

[model]
name = "willcb/Qwen3-8B"

[wandb]
project = "aime-opd"
name = "reflection-pi-self-teacher-8b-v4"

[ckpt]
interval = 10

# --- Trainer ---
[trainer.model]
fused_lm_head_chunk_size = 8192

[trainer.model.lora]
rank = 32
alpha = 32
dropout = 0.05
target_modules = ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]

[trainer.model.ac]
freq = 1

[trainer.loss]
teacher_tau = 1.0
adv_tau = 0.0          # Pure OPD — no GRPO reward signal

[trainer.optim]
lr = 1e-5
weight_decay = 0.0

# --- Orchestrator ---
[orchestrator]
batch_size = 32
rollouts_per_example = 4
oversampling_factor = 2.0
max_concurrent = 16

[orchestrator.sampling]
temperature = 1.0
max_tokens = 31768

[orchestrator.sampling.extra_body]
chat_template_kwargs = { enable_thinking = false }

# Train on AIME 2025 problems 0-21, multi-turn with reflection
[[orchestrator.env]]
id = "aime_mt"
name = "aime-mt"
args = { dataset_name = "aime2025", num_train_examples = 22, student_pi = "binary", reflection_style = "structured" }

[orchestrator.eval]
interval = 10
eval_base_model = false
rollouts_per_example = 4

# Eval uses standard single-turn AIME (no reflection) for fair comparison
[[orchestrator.eval.env]]
id = "aime"
name = "aime-eval-heldout"
args = { dataset_name = "aime2025", eval_dataset = "aime2025", eval_start_index = 22, num_eval_examples = 8 }

[[orchestrator.eval.env]]
id = "aime"
name = "aime-eval-train"
args = { dataset_name = "aime2025", eval_dataset = "aime2025", num_eval_examples = 22 }

[orchestrator.eval.sampling]
temperature = 0.6
max_tokens = 31000

[orchestrator.eval.sampling.extra_body]
chat_template_kwargs = { enable_thinking = false }

[orchestrator.wandb.log_extras]
samples = true
distributions = true
interval = 5

# --- Teacher Config (8B self-teacher, PI from prepare_teacher_context) ---
[orchestrator.teacher_model]
max_model_len = 32768
use_tito = false

[orchestrator.teacher_model.chat_template_kwargs]
enable_thinking = false

# --- Student Inference (8B, 2 GPUs) ---
[inference]
gpu_memory_utilization = 0.85

[inference.model]
name = "willcb/Qwen3-8B"
max_model_len = 32768
dtype = "bfloat16"

# --- Teacher Inference (8B self-teacher, 1 GPU) ---
[teacher_inference]
gpu_memory_utilization = 0.9

[teacher_inference.server]
port = 8001

[teacher_inference.model]
name = "willcb/Qwen3-8B"
max_model_len = 32768
dtype = "bfloat16"
