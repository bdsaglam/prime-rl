# AIME Cross-Teacher OPD Deliberative: 8B student + 32B external teacher (deliberative PI)
# Pure OPD signal (adv_tau=0), no GRPO component.
# Teacher generates analysis of student rollouts before scoring.
# 4× A100 80GB: 1 train + 3 infer. Teacher on external server.

max_steps = 22
seq_len = 32768
output_dir = "outputs/aime-cross-opd-deliberative"

[deployment]
num_train_gpus = 1
num_infer_gpus = 3
# teacher hosted externally — no local teacher GPUs

[model]
name = "willcb/Qwen3-8B"

[wandb]
project = "aime-opd"
name = "aime-cross-opd-deliberative"

[ckpt]
interval = 10

# --- Trainer ---
[trainer.model]
fused_lm_head_chunk_size = 8192

[trainer.model.lora]
rank = 32
alpha = 32
dropout = 0.05
target_modules = ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]

[trainer.model.ac]
freq = 1

[trainer.loss]
teacher_tau = 1.0
adv_tau = 0.0          # Pure OPD — no GRPO reward signal

[trainer.optim]
lr = 1e-5
weight_decay = 0.0

# --- Orchestrator ---
[orchestrator]
batch_size = 128
rollouts_per_example = 4
oversampling_factor = 2.0
max_concurrent = 16

[orchestrator.sampling]
temperature = 1.0
max_tokens = 31768

[[orchestrator.env]]
id = "aime"
name = "aime"
args = { dataset_name = "aimo-validation-aime" }

[orchestrator.eval]
interval = 11
rollouts_per_example = 4

[[orchestrator.eval.env]]
id = "aime"
name = "aime-eval-2025"
args = { dataset_name = "aime2025", eval_dataset = "aime2025", num_eval_examples = 8 }

[orchestrator.eval.sampling]
temperature = 0.6
max_tokens = 31000

[orchestrator.wandb.log_extras]
samples = true
distributions = true
interval = 10

# --- External Teacher (32B on remote server) ---
[orchestrator.teacher_model]
deliberative = true
deliberative_max_tokens = 8192
deliberative_temperature = 0.3
max_model_len = 32768
use_tito = false

[orchestrator.teacher_model.client]
base_url = ["http://localhost:8932/v1"]

[orchestrator.teacher_model.model]
name = "willcb/Qwen3-32B"

# --- Student Inference (8B, 3 GPUs) ---
[inference]
gpu_memory_utilization = 0.85

[inference.model]
name = "willcb/Qwen3-8B"
max_model_len = 32768
dtype = "bfloat16"
